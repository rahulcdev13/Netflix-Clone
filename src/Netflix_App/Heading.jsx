import React from "react"

const Heading = (props) => {
    return (
        <h2 className="card_title">{props.SeriesName}</h2>
    );
}
export default Heading;

/* <h2 className="card_title">{props.SeriesName}</h2> */ 